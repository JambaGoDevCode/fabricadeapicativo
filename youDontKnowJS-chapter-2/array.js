/**
    An array is an object tha holds values (of any type) not particularly
    in nmed properties/keys, but rather in numerically indexed positions.
 */

    var arr = [
        'name',
        23,
        true
    ]

    arr[0]
    arr[1]
    arr[2]
    arr.length

    console.log(arr[0])
    console.log(arr[1])
    console.log(arr[2])
    console.log(arr.length)