

a = b * 2

/**
    This statement has four expressions in it
    
    - 2 is a literal value expression
    - b is a variable expression, which means to ritrieve its current value
    - b * 2 is an arithmetic expression, which means to do multiplication.
    - a = b * 2 is an assignment expression, which means to assign the result of the b * 2 expression to be variable a (more on assignments later)

 */



    // Output 

    a = 28
    b = a * 2
    console.log(b)


    // Input 
    age = prompt("Place tell me you age")
    console.log(age)